# Render Blueprint — crossub_mobile_agent portal
# Docs: https://render.com/docs/blueprint-spec
#
# Deploy: Render Dashboard → New → Blueprint → connect this repo.
# Requires crossub_web API already deployed; set API_INTERNAL_URL after first deploy.

services:
  - type: web
    name: crossub-mobile-agent
    runtime: node
    region: singapore
    plan: starter
    buildCommand: corepack pnpm install --frozen-lockfile && corepack pnpm build:portal
    startCommand: corepack pnpm --filter @crossub/portal start
    healthCheckPath: /login
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: /api
      - key: API_INTERNAL_URL
        sync: false
      - key: NEXT_PUBLIC_TENANT_APP_URL
        sync: false
      - key: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        sync: false
