# Browser-side API base. Default '/api' goes through Next.js rewrites so cookies
# stay same-origin. Requires crossub_web API running (default :3001).
NEXT_PUBLIC_API_URL=/api

# Server-side rewrite target — points at the crossub_web Nest API.
API_INTERNAL_URL=http://localhost:3001
